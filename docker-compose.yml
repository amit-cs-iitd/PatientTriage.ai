services:
  postgres:
    image: postgres:16-alpine
    container_name: triage_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: triage
      POSTGRES_PASSWORD: triage_dev
      POSTGRES_DB: triage_db
    ports:
      - "5432:5432"
    volumes:
      - triage_pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U triage -d triage_db"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  triage_pg_data:
